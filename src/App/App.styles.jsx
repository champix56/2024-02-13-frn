import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  demat: {
    color: 'blue',
    fontWeight: 'bold',
    fontSize: 32,
    textAlign: 'center',
  },
});

export default styles;
